我要做一个广东高考英语听说考试提分网站，帮我设计并生成全部前端页面和 UI 组件。

## 技术栈

- Vue 3 + TypeScript + Vite
- CSS Custom Properties（不用 Tailwind）
- Chart.js + vue-chartjs
- 不涉及登录系统，纯单用户工具

## 项目背景

广东高考英语听说考试满分 60 分，占英语科目 10%。考试有三部分：
- **Part A 模仿朗读**：看一段英文（~100 词），朗读出来
- **Part B 角色扮演**：给一个场景，提 3 个问题 + 回答 5 个问题
- **Part C 故事复述**：听一段故事（~200 词），复述出来

目标用户是广东高中生，三类人：
1. **高三冲刺型**：时间紧，想快速看技巧，碎片时间学习
2. **高一/高二训练型**：系统提升，愿意开口练习
3. **基础薄弱型**：需要鼓励，从简单开始建立信心

核心体验：**不只是"练了"，而是"会了"** —— 每次练习后告诉学生哪里弱、为什么弱、怎么改。

## 设计风格

关键词：**学习 · 专注 · 亲和 · 不焦虑 · 不花哨**

参考方向：Notion 的干净卡片布局、Duolingo 的色彩运用（但不是卡通风格）、Coursera 的进度展示。

这是备考工具，不是游戏。目标不是"酷"，而是让学生觉得"这个工具能帮我提分"。

### 颜色体系（已锁定，请直接使用）

主色是墨绿松石（稳重、学习感），强调色是橙色（得分、重点操作）：

```css
--color-primary: #0f766e;
--color-primary-light: #14b8a6;
--color-primary-dark: #115e59;
--color-accent: #f97316;
--color-accent-light: #fb923c;
--color-bg: #f6f7f4;
--color-surface: #ffffff;
--color-surface-hover: #edf4f2;
--color-border: #dbe7e3;
--color-text: #1f2937;
--color-text-secondary: #4b5563;
--color-text-muted: #9ca3af;
--color-success: #22c55e;
--color-warning: #f59e0b;
--color-error: #ef4444;
--color-part-a: #0284c7;   /* Part A 蓝色 */
--color-part-b: #0f766e;   /* Part B 绿色 */
--color-part-c: #ea580c;   /* Part C 橙红 */
```

圆角：小 6px、中 10px、大 16px、超大 24px
阴影：浅 sm、中 md、深 lg
字体：英文 Poppins、中文 Noto Sans SC

### 动效原则

- 页面切换 fade + 轻微上移（200ms）
- 卡片 hover 微上移 + 阴影加深（150ms）
- 按钮点击微缩放（100ms）
- **禁止**弹跳动画、旋转装饰、闪烁效果（不严肃）

### 响应式

- 移动端 375px 1 列
- 平板 768px 2 列
- 桌面 1024px+ 3 列
- 主内容区 max-width: 960px，居中
- 导航栏移动端考虑折叠菜单

## 需要输出的页面（共 8 页）

每个页面输出一个 `.vue` 文件。样式用 `<style scoped>`，引用上面的 CSS 变量。
**不需要实现业务逻辑**，只出 UI 框架和样式。

### 1. 首页（/）

引导用户到两个核心入口。

内容：
- Hero 区：标题"不只是'练了'，而是'会了'" + 简短说明
- 两个大按钮："快速学习（不开口）" 和 "去练习（开口训练）"
- 三个功能简介卡片（快速学习 / 专项练习 / 进度追踪），各带跳转链接
- 我的近期数据：总练习次数、平均分、最近记录数（三个数字卡片）
- 非 Chrome 浏览器用户看到提示条

状态要求：
- 首次使用数据为 0 时显示"还没有练习记录"
- 有数据时显示具体数字

### 2. 学习中心列表页（/learn）

按分类浏览技巧文章。

内容：
- 顶部标题 + 引导文案
- 6 个分类 Tab（可横向滑动）：考前速成🎯、发音突破🗣、语法急救📝、模板工具箱🏗、场景词汇包📖、实战技巧🎬
- 当前分类的名称和描述
- 文章卡片网格：每张显示阅读时间标签、难度标签、标题、简介、"阅读文章"按钮

状态要求：
- 分类无文章时显示"暂无内容"
- Tab 切换平滑过渡

### 3. 学习文章详情页（/learn/:category/:id）

阅读一篇技巧文章。

内容：
- 返回链接 + 文章标题 + 副标题 + 阅读时间/难度标签
- 正文区，文章内容有 6 种块类型，每种样式不同：
  - text：纯文本段落
  - heading：小标题，加粗
  - comparison：**核心特色** —— 错误 vs 正确对比框（左红/右绿或上下结构）
  - tip：速记提示（浅色背景）
  - example：示例句子（等宽字体）
  - template：模板块（虚线边框）
- 一句话速记卡（橙色边框，截图友好）
- 相关文章推荐链接
- "去练练看"底部按钮

状态要求：文章不存在时显示 404 + 返回按钮

### 4. Part A 练习页（/practice/part-a/:id?）

模仿朗读练习。

两个状态：

**选择文章**：卡片网格显示标题、话题、难度、词数 → 点击进入练习

**练习中**：
- 短文展示（清晰大字号、行距宽）
- 操作按钮："播放示范朗读" / "开始朗读" / "停止"
- 练习时长计时器
- 语音识别实时转写展示区（interim + final）
- 备选 textarea（浏览器不支持语音时使用）
- "提交并生成诊断"按钮
- 浏览器不支持时的橙色提示条

### 5. Part B 练习页（/practice/part-b/:id?）

角色扮演练习。

两个状态：选择场景 → 练习中

练习中：
- 场景描述头
- 提问环节（3 题）：每题显示中文提示 + 参考英文 + 输入框
- 回答环节（5 题）：每题显示英文问题 + 参考回答 + 输入框
- 计时器
- 环节切换指示器（可选，显示当前在第几步）

### 6. Part C 练习页（/practice/part-c/:id?）

故事复述练习。

两个状态：选择故事 → 练习中

练习中：
- 故事标题 + 框架提示
- "播放故事（两遍）"按钮，显示播放进度
- 复述计时器
- 复述输入区（同 Part A 的语音 + 手动模式）
- 提交按钮

### 7. 练习结果页（/result/:attemptId）

看完即走的诊断报告。

内容：
- 总分大号数字 + 等级徽章（优秀/良好/及格/需加强）
- 每个 Part 的结果卡片：
  - Part 编号 + 分数
  - 各维度水平进度条（标签 + 百分比）
  - 诊断建议区：✅ 做得好的、⚠️ 需加强的、🔍 具体错误列表（错误内容 → 改进建议 → 推荐文章链接）
- "继续练习"按钮

### 8. 进度页（/progress）

学习数据和能力画像。

内容：
- 三个统计数据卡片：总练习次数 / 平均分 / 最近记录数
- 能力画像：5 维雷达图（发音、流利度、语法、词汇、内容复述）+ 各维度数值
- 练习历史：按时间倒序的列表（日期 + 模式 + 分数 + 用时）
- 分数趋势折线图

状态要求：
- 无数据时显示"还没有练习记录，先去完成一次训练吧"
- 数据很少时图表正常显示

## 通用组件（也请输出）

除了 8 个页面，还需要以下复用组件，每个一个 .vue 文件：

| 组件 | 说明 |
|------|------|
| AppHeader.vue | 顶部导航栏：logo + 4 个导航链接，当前页高亮 |
| ScoreBar.vue | 水平进度条：标签 + 进度条 + 百分比数值 |
| CountdownTimer.vue | 倒计时/正计时显示 |
| SpeechInput.vue | 语音识别按钮 + textarea 组合输入 |
| StatsCard.vue | 数字统计卡片（大数字 + 标签） |
| DiagnosisCard.vue | 诊断建议卡片（错误 + 建议 + 推荐文章） |

## 项目文件结构参考

```
src/
├── assets/styles/
│   ├── variables.css    ← 颜色变量文件（已有，引用即可）
│   └── main.css         ← 全局样式（已有，.btn-primary 等直接用）
├── components/
│   ├── layout/          ← AppHeader.vue 放这里
│   ├── common/          ← 通用组件放这里
│   ├── learn/
│   ├── partA/
│   ├── partB/
│   ├── partC/
│   └── mockExam/
└── views/               ← 8 个页面放这里
```

## 特别提醒

- 不要引入新的主色调，用上面给的颜色变量
- 不用 Tailwind，用 `<style scoped>` + CSS 变量
- 不出业务逻辑（methods 里留空或写 // TODO）
- props 用 defineProps 声明类型
- 所有按钮用已有的全局类：`.btn-primary` `.btn-secondary` `.card`
- 组件要覆盖加载态、空态、错误态等边缘情况
- 移动端菜单折叠请考虑（用 ref + v-if 控制显示/隐藏）
