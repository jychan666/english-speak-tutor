# 非前端任务清单

> 以下任务均不涉及 `client/src/views/`、`client/src/components/`、`client/src/assets/styles/` 的修改。

---

## 任务 1：后端 - 设备令牌系统

**文件**：`server/src/middleware/deviceToken.ts`（新建）

**目标**：实现无登录的数据隔离。每个浏览器/设备分配一个 UUID，通过 HTTP 头传递。

**要求**：
1. 中间件从请求头 `X-Device-Token` 读取令牌
2. 如果没有或格式不对，生成新 UUID 并通过响应头 `X-Device-Token` 返回
3. 将 token 挂载到 `req.deviceToken` 供下游路由使用
4. 在 `server/src/index.ts` 中注册（CORS 之后、路由之前）

---

## 任务 2：后端 - 按设备隔离存储

**文件**：`server/src/db/database.ts`（修改）

**目标**：当前所有数据存一个 `attempts.json`。改为每个设备令牌独立文件。

**要求**：
1. 数据目录改为 `server/data/attempts/{token}.json`
2. `listAttempts(deviceToken, limit)` — 读取特定设备的记录
3. `upsertAttempt(deviceToken, attempt)` — 写入特定设备的文件
4. 新设备首次访问自动创建空文件
5. 保持现有函数签名向后兼容（加参数，不加改名）

---

## 任务 3：后端 - 练习内容 API

**文件**：`server/src/routes/content.ts`（新建）

**目标**：提供练习材料的服务端 API，方便后续 CDN 缓存和远程更新。

**要求**：
1. `GET /api/content/list` → 返回所有练习材料摘要（id/type/title/topic/difficulty/wordCount）
2. `GET /api/content/:type/:id` → 返回单篇完整内容
3. 数据从客户端 `client/src/data/examContent.ts` 提取，转存为 `server/src/data/practiceContent.json`
4. 路由用 `fs.readFileSync` 读 JSON，不用 import（支持运行时热更新）

**注册**：在 `server/src/index.ts` 中 `app.use('/api/content', contentRoutes)`

---

## 任务 4：后端 - 进度/技能路由适配设备令牌

**文件**：`server/src/routes/progress.ts`、`server/src/routes/skills.ts`（修改）

**目标**：让现有路由使用设备令牌读写对应设备的数据。

**要求**：
1. `progress.ts` 中 `listAttempts()` 和 `upsertAttempt()` 调用改为传入 `req.deviceToken`
2. `skills.ts` 中读取改为传入 `req.deviceToken`
3. 设备令牌不存在时（req.deviceToken 为空），返回空数据而非报错

---

## 任务 5：前端 - localStorage 接入设备令牌

**文件**：`client/src/utils/api.ts`（修改）

**目标**：前端 axios 实例自动附带设备令牌。

**要求**：
1. 首次加载时检查 `localStorage.getItem('device-token')`
2. 如果没有，从服务端响应头 `X-Device-Token` 获取并存储
3. 每次请求在 header 中发送 `X-Device-Token`
4. 请求拦截器自动附加，响应拦截器自动更新

---

## 任务 6：根目录 package.json - 新增脚本

**文件**：`package.json`（修改）

**要求**：
1. 新增 `"db:seed"` 脚本指向 `npm run seed -w server`
2. 新增 `"db:reset"` 脚本用于清空数据（方便开发调试）

---

## 执行顺序

```
任务1 → 任务2 → 任务3 → 任务4 → 任务5 → 任务6
```

1 和 3 可并行。2 依赖 1。4 依赖 1+2。5 依赖 1。

## 约束
- 不修改任何客户端视图/组件/样式文件
- TypeScript 严格模式
- 路由返回格式统一：`{ success: boolean, data?: T, error?: string }`
